import { files as library, projects, users } from "@/lib/mock-data"; import { FileUploadArea } from "@/components/files/file-upload-area"; import { PageHeader } from "@/components/shared/page-header"; import { Button } from "@/components/ui/button";
export default function FilesPage(){ return <div className="space-y-6"><PageHeader title="Files" description="Access shared documents, mock uploads, and linked assets." action={<Button>Upload file</Button>} /><FileUploadArea /><div className="overflow-x-auto rounded-2xl border dark:border-slate-800"><table className="min-w-full text-sm"><thead className="bg-slate-50 dark:bg-slate-900"><tr className="text-left"><th className="p-4">Name</th><th className="p-4">Type</th><th className="p-4">Project</th><th className="p-4">Uploader</th><th className="p-4">Date</th></tr></thead><tbody>{library.map(file=><tr key={file.id} className="border-t dark:border-slate-800"><td className="p-4 font-medium">{file.name}</td><td className="p-4">{file.type}</td><td className="p-4">{projects.find(p=>p.id===file.projectId)?.name}</td><td className="p-4">{users.find(u=>u.id===file.uploaderId)?.name}</td><td className="p-4">{file.uploadedAt}</td></tr>)}</tbody></table></div></div>; }
