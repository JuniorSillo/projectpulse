import { milestones, projects } from "@/lib/mock-data"; import { PageHeader } from "@/components/shared/page-header"; import { Button } from "@/components/ui/button"; import { Progress } from "@/components/ui/progress"; import { StatusBadge } from "@/components/shared/badges";
export default function MilestonesPage(){ return <div className="space-y-6"><PageHeader title="Milestones" description="Monitor major delivery checkpoints across the portfolio." action={<Button>Create Milestone</Button>} /><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{milestones.map(m=>{const p=projects.find(x=>x.id===m.projectId); return <div key={m.id} className="card-ui p-5"><div className="flex items-start justify-between gap-3"><div><h3 className="font-semibold">{m.title}</h3><p className="text-sm text-slate-500">{p?.name}</p></div><StatusBadge value={m.status} /></div><div className="mt-5 space-y-2"><Progress value={m.progress} /><div className="flex justify-between text-sm text-slate-500"><span>{m.progress}% complete</span><span>{m.dueDate}</span></div></div></div>})}</div></div>; }
