"use client";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { ResponsiveContainer, LineChart, Line, CartesianGrid, PieChart, Pie, Cell, Tooltip, XAxis, YAxis, BarChart, Bar } from "recharts";
const palette = ["#484BF1","#0F3FC2","#052A90","#A2AAFF","#C5CBFF"];
export function LineChartCard({ title, data }: { title:string; data:any[] }) { return <Card><CardHeader><CardTitle>{title}</CardTitle></CardHeader><CardContent className="h-72"><ResponsiveContainer width="100%" height="100%"><LineChart data={data}><CartesianGrid strokeDasharray="3 3" opacity={0.15} /><XAxis dataKey="week" /><YAxis /><Tooltip /><Line type="monotone" dataKey="progress" stroke="#484BF1" strokeWidth={3} /></LineChart></ResponsiveContainer></CardContent></Card>; }
export function PieChartCard({ title, data }: { title:string; data:any[] }) { return <Card><CardHeader><CardTitle>{title}</CardTitle></CardHeader><CardContent className="h-72"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={data} dataKey="value" nameKey="name" outerRadius={90}>{data.map((_:any, i:number) => <Cell key={i} fill={palette[i % palette.length]} />)}</Pie><Tooltip /></PieChart></ResponsiveContainer></CardContent></Card>; }
export function BarChartCard({ title, data }: { title:string; data:any[] }) { return <Card><CardHeader><CardTitle>{title}</CardTitle></CardHeader><CardContent className="h-72"><ResponsiveContainer width="100%" height="100%"><BarChart data={data}><CartesianGrid strokeDasharray="3 3" opacity={0.15} /><XAxis dataKey="name" /><YAxis /><Tooltip /><Bar dataKey="value" fill="#484BF1" radius={[8,8,0,0]} /></BarChart></ResponsiveContainer></CardContent></Card>; }
