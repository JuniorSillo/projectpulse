import { comments, users } from "@/lib/mock-data"; import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
export function ActivityFeed(){ return <Card><CardHeader><CardTitle>Recent activity</CardTitle></CardHeader><CardContent className="space-y-4">{comments.map(c=>{const a=users.find(u=>u.id===c.authorId); return <div key={c.id} className="flex gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-full bg-brand-primary/10 text-sm font-semibold text-brand-primary">{a?.avatar ?? "PP"}</div><div><p className="text-sm"><span className="font-medium">{a?.name}</span> {c.message}</p><p className="text-xs text-slate-500">{new Date(c.createdAt).toLocaleString()}</p></div></div>})}</CardContent></Card>; }
